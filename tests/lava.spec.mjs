import {test,expect} from '@playwright/test';
test('placing a Mathmos lamp creates a light and retains bottle colour and base options',async({page})=>{
 await page.goto('/demo/');await page.waitForFunction(()=>!document.documentElement.hasAttribute('data-loading'));await page.getByRole('button',{name:'Edit layout',exact:true}).click();const editor=page.locator('floorplan-card-editor');await editor.getByRole('button',{name:'3. Furniture',exact:true}).click();await editor.getByRole('button',{name:'Unlock editing',exact:true}).click();await editor.getByLabel('Find furniture').fill('Mathmos');await expect(editor.locator('.furniture-palette button')).toHaveCount(4);await editor.locator('.furniture-palette').getByRole('button',{name:/Mathmos Astro Vinyl/}).click();await editor.getByRole('button',{name:'Place furniture in centre',exact:true}).click();await editor.getByLabel('Lava bottle colour').selectOption('0');
 const object=await editor.evaluate(e=>e.config.floors[0].objects.find(o=>o.product_id==='mathmos-vinyl'));expect(object.light_entity).toMatch(/^light\./);expect(object.lava_colour).toBe(0);expect(await editor.evaluate((e,id)=>e.config.floors[0].entities.some(m=>m.entity===id),object.light_entity)).toBe(true);
 await editor.getByLabel('Product preset',{exact:true}).selectOption('mathmos-platinum-vinyl');await expect(editor.getByLabel('Lava bottle colour')).toHaveValue('0');await editor.getByRole('button',{name:'3D preview',exact:true}).click();await expect(editor.locator('canvas')).toBeVisible();
});
